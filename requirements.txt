# Version ceilings below are intentional, not laziness: this repo targets the
# LangChain 0.3.x "LCEL" API (create_retrieval_chain, create_history_aware_retriever,
# RunnableWithMessageHistory, etc). LangChain 1.0 (and the matching 1.x releases of
# langgraph / langchain-anthropic / langchain-chroma / langchain-mcp-adapters) shipped
# a breaking redesign of these APIs. Pin below 1.0 until this repo is migrated.

# Core
python-dotenv>=1.0.1
langchain>=0.3.7,<1.0
langchain-core>=0.3.15,<1.0
langchain-community>=0.3.5,<1.0
langgraph>=0.2.45,<0.4

# Anthropic (primary/reference provider)
langchain-anthropic>=0.3.0,<1.0
anthropic>=0.39.0

# Other chat providers (optional, used only in "using a different model" notes)
langchain-openai>=0.2.6,<1.0
langchain-google-genai>=2.0.4,<3.0
langchain-ollama>=0.2.0,<1.0

# Embeddings
langchain-voyageai>=0.1.3,<1.0
voyageai>=0.3.0
sentence-transformers>=3.2.1

# Vector stores
faiss-cpu>=1.9.0
chromadb>=0.5.18
langchain-chroma>=0.1.4,<1.0

# Document loading / splitting
pypdf>=5.0.1
langchain-text-splitters>=0.3.2,<1.0
beautifulsoup4>=4.12.3

# Multimodal helper (generates the sample image on the fly)
pillow>=11.0.0

# MCP
mcp>=1.1.0
langchain-mcp-adapters>=0.0.5,<0.2
httpx>=0.27.2

# Observability
langsmith>=0.1.147
